// Re-exported from shared/ so the API route and the /site-health client page
// compute findings from the exact same pure function - see
// shared/siteHealthFindings.ts for the rules and point-value derivation.
export * from "@shared/siteHealthFindings";
