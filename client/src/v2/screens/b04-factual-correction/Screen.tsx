import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";

export type Board04Data = Record<string, never>;

export function Board04Screen(_props: V2ScreenProps<Board04Data>) {
  return <NotBuilt board="b04" title="My work - factual correction (Guided)" />;
}
