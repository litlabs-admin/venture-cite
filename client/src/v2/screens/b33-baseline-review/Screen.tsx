import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board33Data = Record<string, never>;
export function Board33Screen(_props: V2ScreenProps<Board33Data>) {
  return <NotBuilt board="b33" title="Onboarding - first baseline review (Guided)" />;
}
