import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";

export type Board05Data = Record<string, never>;

export function Board05Screen(_props: V2ScreenProps<Board05Data>) {
  return <NotBuilt board="b05" title="My work - confirmation gate (Guided)" />;
}
