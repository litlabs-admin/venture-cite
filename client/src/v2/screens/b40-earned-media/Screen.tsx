import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board40Data = Record<string, never>;
export function Board40Screen(_props: V2ScreenProps<Board40Data>) {
  return <NotBuilt board="b40" title="Earned media opportunities (Guided)" />;
}
