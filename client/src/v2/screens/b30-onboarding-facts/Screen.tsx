import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board30Data = Record<string, never>;
export function Board30Screen(_props: V2ScreenProps<Board30Data>) {
  return <NotBuilt board="b30" title="Onboarding - fact review" />;
}
