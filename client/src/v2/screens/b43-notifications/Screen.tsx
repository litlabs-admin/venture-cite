import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board43Data = Record<string, never>;
export function Board43Screen(_props: V2ScreenProps<Board43Data>) {
  return <NotBuilt board="b43" title="Notifications and inbox (Guided)" />;
}
