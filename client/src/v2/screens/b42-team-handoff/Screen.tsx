import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board42Data = Record<string, never>;
export function Board42Screen(_props: V2ScreenProps<Board42Data>) {
  return <NotBuilt board="b42" title="Team and handoff (Guided)" />;
}
