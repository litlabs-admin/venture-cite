import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board16Data = Record<string, never>;
export function Board16Screen(_props: V2ScreenProps<Board16Data>) {
  return <NotBuilt board="b16" title="My work - publication check (Guided)" />;
}
