import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board24Data = Record<string, never>;
export function Board24Screen(_props: V2ScreenProps<Board24Data>) {
  return <NotBuilt board="b24" title="Settings (Guided)" />;
}
