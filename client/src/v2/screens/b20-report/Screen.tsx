import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board20Data = Record<string, never>;
export function Board20Screen(_props: V2ScreenProps<Board20Data>) {
  return <NotBuilt board="b20" title="Visibility - report (Guided)" />;
}
