import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board25Data = Record<string, never>;
export function Board25Screen(_props: V2ScreenProps<Board25Data>) {
  return <NotBuilt board="b25" title="Integrations (Guided)" />;
}
