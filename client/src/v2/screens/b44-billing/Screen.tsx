import type { V2ScreenProps } from "@/v2/contracts/screen";
import { NotBuilt } from "../_placeholder/NotBuilt";
export type Board44Data = Record<string, never>;
export function Board44Screen(_props: V2ScreenProps<Board44Data>) {
  return <NotBuilt board="b44" title="Billing and usage (Guided)" />;
}
