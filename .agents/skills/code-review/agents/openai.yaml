interface:
  display_name: "Code Review"
  short_description: "Review a diff on standards and spec"
  default_prompt: "Use $code-review to review this diff against standards and the requested behavior."
policy:
  allow_implicit_invocation: true
