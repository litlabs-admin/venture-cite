interface:
  display_name: "Research"
  short_description: "Research from high-trust sources"
  default_prompt: "Use $research to investigate this question from primary sources."
policy:
  allow_implicit_invocation: true
