interface:
  display_name: "Prototype"
  short_description: "Prototype to answer a design question"
  default_prompt: "Use $prototype to test this design question with a throwaway prototype."
policy:
  allow_implicit_invocation: true
