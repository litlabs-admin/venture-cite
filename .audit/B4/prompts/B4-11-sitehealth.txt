Extract the site health domain into `shared/schema/siteHealth.ts`.

Eight modules already exist: identity, brands, content, prompts, competitors,
perception, chatbot, factAgent. Follow their pattern exactly.

## What moves

The `siteHealthScanHistory` and `siteHealthFindingStatus` tables, wherever they
now sit in `shared/schema.ts`. Locate them; line numbers have shifted.

## References

`siteHealthFindingStatus` references `brands` and `users`.
`siteHealthScanHistory` references `brands` and `brandFactScrapeRuns`.

`brandFactScrapeRuns` now lives in `shared/schema/factAgent.ts`, and the barrel
already imports it from there. So this module imports `brands` from `./brands`,
`users` from `./identity`, and `brandFactScrapeRuns` from `./factAgent`.

Check that this creates no cycle: `factAgent` must not import from `siteHealth`.
If it would, stop and say so in the report rather than working around it.

## Membership completeness

After moving, search `shared/schema.ts` for both table names and confirm the only
remaining references are foreign keys from other tables.

## Clean up after yourself, but only after yourself

Delete every temporary file you create. Do not delete anything you did not
create. Files under `.audit/B4/prompts/` are inputs.

## What must not change

No column, type, name or default changes. No new migration. No consumer edits.

## Acceptance

Run `node scripts/verifySchemaSplit.mjs`. All six gates must pass.

If the SQL gate fails you changed the schema. Find what and fix it.

## Report

Write `.audit/B4/B4-11-sitehealth-report.md`: the tables you located, the
membership check result, the import direction and your cycle check, the gate
output, and whether any consumer file changed.

## This run is non-interactive

Nobody can answer you. Decide, implement, verify, report. Do not end your turn
with a question.
