Extract the content domain into `shared/schema/content.ts`.

Third of seven. Two modules already exist, `identity.ts` and `brands.ts`. Follow
the pattern they set.

## What moves

Lines 77 to 313 of `shared/schema.ts`, a contiguous block. Confirm the boundary
yourself; a comment block above a table belongs to that table, not the one
before it.

Tables: `articles`, `articleRevisions`, `distributions`, `keywordResearch`,
`contentGenerationJobs`, with the zod builders and type aliases that sit beneath
each of them.

`llmJobs` at line 314 does NOT move. It is generic job infrastructure and goes to
`platform` later, even though it is adjacent.

## References

`articles` references `brands`. The others reference `articles`, `brands` and
`users`. So `content.ts` imports from `brands.ts` and `identity.ts`, and neither
imports back. Keep the dependency one directional.

`articles` is referenced by 8 foreign keys from tables still in the barrel, so it
must be importable as a value there, the same way `brands` and `users` are.

## Membership completeness

The identity extraction left `insertUserSchema` behind in the barrel, and every
gate passed anyway: a declaration in the wrong file still exports, compiles, and
type-checks. The gates prove nothing broke; they do not prove the move was
complete.

So check explicitly. After moving, no companion of any of the five tables may
remain in `shared/schema.ts`. Search for each table name and confirm the only
remaining references are foreign keys from other tables.

## What must not change

No column, type, name or default changes. No new migration. No consumer edits.

## Acceptance, in this order

1. `npm run schema:surface:check` prints "Export surface unchanged. 260 exports."
2. Generated SQL unchanged. Generate from `git show HEAD:shared/schema.ts` and
   from the working tree into separate directories with
   `DATABASE_URL="postgresql://u:p@127.0.0.1:1/none" npx drizzle-kit generate --out=<dir> --schema=<file> --dialect=postgresql`,
   split both on `--> statement-breakpoint`, normalise whitespace, sort, diff.
   Identical, 271 statements each. Delete any temporary file you create.
3. `npm run check`
4. `npm run lint` and `npm run format:check`
5. `npm test -- --maxWorkers=1`

If the SQL differs you changed the schema. Find what and fix it. Do not explain
it away.

## Report

Write `.audit/B4/B4-04-content-report.md`: the boundary you confirmed, the
membership check and its result, the import direction, the five gate outputs,
`git diff --stat`, and whether any consumer file changed.

## This run is non-interactive

Nobody can answer you. Decide, implement, verify, report. Do not end your turn
with a question.
