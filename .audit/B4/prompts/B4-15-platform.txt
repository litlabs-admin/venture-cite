Extract the platform domain into `shared/schema/platform.ts`.

Nine modules already exist: identity, brands, content, prompts, competitors,
perception, chatbot, factAgent, siteHealth. Follow their pattern exactly. Move
tables with the zod builders and type aliases beneath them, re-export from the
barrel with `export *`, keep imports one directional.

## What moves

Everything that remains in the barrel after the other twelve modules are extracted. Verify the list against `.audit/B4/PARTITION.md` and report any table present in the barrel that the partition does not name.

These tables are not contiguous. Locate each one; line numbers have shifted with
every extraction. A comment block above a table belongs to that table.

Tables: `analytics`, `metricsHistory`, `alertSettings`, `alertHistory`, `communityPosts`, `emailFailures`, `apiCosts`, `auditLogs`, `notificationPreferences`, `schemaAudits`, `competitorFavicons`, `sourceHealth`, `sentimentCache`, `tourEvents`, `systemState`

## References

`alertHistory` references `alertSettings` and `brands`. `metricsHistory`, `alertSettings`, `communityPosts` and `sourceHealth` reference `brands`. `tourEvents` references `brands` and `users`. The rest reference nothing. So this module imports from `./brands` and `./identity`. After this module lands, `shared/schema.ts` should contain no `pgTable` call at all and exist only as a barrel of re-exports. Say in your report whether that is true.

Any table you move that is still referenced by a foreign key from the barrel must
remain importable there, the way the barrel already imports `brands`, `users`
and `brandFactScrapeRuns`.

Check for cycles before you finish. No module already extracted may import from
platform. If that would be required, stop and say so in the report rather than working
around it.

## Membership completeness

The identity extraction left `insertUserSchema` behind and every gate still
passed, because a declaration in the wrong file exports, compiles and
type-checks. After moving, search `shared/schema.ts` for each table name and
confirm the only remaining references are foreign keys from other tables.

## Clean up after yourself, but only after yourself

Delete every temporary file you create. Do not delete anything you did not
create. Files under `.audit/B4/prompts/` are inputs, not temporary work.

## What must not change

No column, type, name or default changes. No new migration. No consumer edits.

## Acceptance

Run `node scripts/verifySchemaSplit.mjs`. All six gates must pass.

If the SQL gate fails you changed the schema. Find what and fix it. Do not
explain it away.

## Report

Write `.audit/B4/B4-15-platform-report.md`: the tables you located, the membership check
result, the import direction and cycle check, the gate output, and whether any
consumer file changed.

## This run is non-interactive

Nobody can answer you. Decide, implement, verify, report. Do not end your turn
with a question.
