# Verification batch V02

Findings examined: 9
CONFIRMED: 9   REFUTED: 0   UNCERTAIN: 0

## F-S02-server-jobs-003 | CONFIRMED
Original claim: A failed platform check is stored as an uncited ranking.
What I checked: `server/citationChecker.ts:663-717, 879-896, 1116-1140, 1229-1274`; `server/routes/prompts.ts:1064-1165`.
Verdict reasoning: The retry failure sets `fetchError`. The matcher then returns false. The code stores `isCited: 0`. One stored row makes the run succeed.
Reachable path (CONFIRMED only): `POST /api/brand-prompts/:brandId/run` at `server/routes/prompts.ts:1072` -> kickoff at `server/citationChecker.ts:1418` -> failed fetch at `server/citationChecker.ts:703-717` -> row at `server/citationChecker.ts:1116-1139` -> success at `server/citationChecker.ts:1252-1274`.
Corrected severity (CONFIRMED only): high. The result corrupts a core citation metric.

## F-S02-server-jobs-005 | CONFIRMED
Original claim: A kickoff run stays running when all prompts are paused.
What I checked: `server/citationChecker.ts:409-467, 1358-1434, 1441-1471`; `server/routes/prompts.ts:1073-1165`; `migrations/0035_citation_runs_dedup.sql:24-26`; `server/databaseStorage.ts:1379-1391`.
Verdict reasoning: Kickoff creates a running row. All paused prompts make `prompts` empty. The early return does not update the supplied run. The active-row index then blocks later kickoffs.
Reachable path (CONFIRMED only): `POST /api/brand-prompts/:brandId/run` at `server/routes/prompts.ts:1073` -> row creation at `server/citationChecker.ts:1380` -> early return at `server/citationChecker.ts:466-467` -> active index at `migrations/0035_citation_runs_dedup.sql:24-26`.
Corrected severity (CONFIRMED only): high. The brand cannot start another citation run.

## F-S12-002 | CONFIRMED
Original claim: A citation run checks the budget only once before all provider calls.
What I checked: `server/citationChecker.ts:437-444, 663-735`; `server/citationChecker.ts:363-371`; `server/lib/llmBudget.ts:30-57, 73-110`; `server/routes/prompts.ts:1072-1115`.
Verdict reasoning: `assertWithinBudget` runs once before the queue starts. Each provider response only records spend. No later task checks the remaining budget.
Reachable path (CONFIRMED only): `POST /api/brand-prompts/:brandId/run` at `server/routes/prompts.ts:1072` -> budget check at `server/citationChecker.ts:437-444` -> queued calls at `server/citationChecker.ts:663-735` -> spend record at `server/citationChecker.ts:363-371`.
Corrected severity (CONFIRMED only): high. One run can exceed the daily token limit.

## F-S12-004 | CONFIRMED
Original claim: A run succeeds when it stores rows from failed provider checks.
What I checked: `server/citationChecker.ts:703-717, 879-896, 1116-1140, 1229-1274`; `server/routes/prompts.ts:1072-1165`.
Verdict reasoning: Failed checks still store ranking rows. Final status uses row count only. It does not inspect failure context or `fetchError`.
Reachable path (CONFIRMED only): `POST /api/brand-prompts/:brandId/run` at `server/routes/prompts.ts:1072` -> failed fetch at `server/citationChecker.ts:703-717` -> stored row at `server/citationChecker.ts:1116-1139` -> success at `server/citationChecker.ts:1252-1274`.
Corrected severity (CONFIRMED only): high. A provider outage becomes a false successful run.

## F-S12-005 | CONFIRMED
Original claim: Resume mode treats failed checks as completed checks.
What I checked: `server/citationChecker.ts:620-648, 703-717, 1116-1140, 1441-1471`; `server/routes/prompts.ts:1290-1325`.
Verdict reasoning: Resume adds every existing ranking key to `alreadyDone`. Failure rows have the same key. The queue skips them without checking failure state.
Reachable path (CONFIRMED only): failed row at `server/citationChecker.ts:1116-1139` -> `POST /citation-runs/:runId/advance` at `server/routes/prompts.ts:1293` -> resume at `server/citationChecker.ts:1461-1465` -> skip at `server/citationChecker.ts:620-648`.
Corrected severity (CONFIRMED only): high. A transient provider failure becomes permanent run data.

## F-S08-001 | CONFIRMED
Original claim: A stale full-board save can overwrite another user's update.
What I checked: `client/src/pages/internal/Board.tsx:45-54, 82-107`; `server/routes/board.ts:96-127`; `tests/unit/boardRoutes.test.ts:47-69`.
Verdict reasoning: Each client sends its full local list. The server unconditionally upserts that list. Neither side sends or checks a version.
Reachable path (CONFIRMED only): local change at `client/src/pages/internal/Board.tsx:100-107` -> full PUT at `client/src/pages/internal/Board.tsx:45-54` -> unconditional upsert at `server/routes/board.ts:117-123`.
Corrected severity (CONFIRMED only): high. A concurrent public board edit can be lost.

## F-S08-002 | CONFIRMED
Original claim: An empty saved board reloads as the seed board.
What I checked: `client/src/pages/internal/Board.tsx:34-42, 82-107`; `server/routes/board.ts:96-127`; `tests/unit/boardRoutes.test.ts:47-69`.
Verdict reasoning: The server returns `[]`. `loadBoard` converts `[]` to null. Mount keeps the seed list. The ready save effect then stores the seed list.
Reachable path (CONFIRMED only): empty PUT at `server/routes/board.ts:117-123` -> GET at `server/routes/board.ts:96-101` -> null at `client/src/pages/internal/Board.tsx:39` -> seed at `client/src/pages/internal/Board.tsx:87-91` -> PUT at `client/src/pages/internal/Board.tsx:100-107`.
Corrected severity (CONFIRMED only): high. A valid empty shared board is silently replaced.

## F-S08-004 | CONFIRMED
Original claim: The board renders unvalidated ticket links as anchors.
What I checked: `client/src/pages/internal/Board.tsx:154-162, 368-374, 526-529`; `client/src/lib/urlSafety.ts:4-15, 38-42`; `server/routes/board.ts:51-74`.
Verdict reasoning: Import and editing accept arbitrary strings. The card passes `t.link` directly to `href`. The available helper is not used.
Reachable path (CONFIRMED only): import at `client/src/pages/internal/Board.tsx:154-162` or link input at `client/src/pages/internal/Board.tsx:526-529` -> raw anchor at `client/src/pages/internal/Board.tsx:368-374`.
Corrected severity (CONFIRMED only): medium. The user must click the hostile link. `noopener` prevents direct access to the opener.

## F-S08-005 | CONFIRMED
Original claim: JSON import accepts invalid array items and can crash the board render.
What I checked: `client/src/pages/internal/Board.tsx:110-124, 154-162`; `client/src/pages/internal/types.ts:9-35`; `server/routes/board.ts:51-74`; `tests/unit/boardRoutes.test.ts:82-98`.
Verdict reasoning: The cast does not validate JSON. `[null]` passes `Array.isArray`. The next render reads `t.column` in the filter and throws.
Reachable path (CONFIRMED only): JSON import at `client/src/pages/internal/Board.tsx:154-162` -> `[null]` state -> filter at `client/src/pages/internal/Board.tsx:110-124` -> `TypeError`.
Corrected severity (CONFIRMED only): low. The user must import the file. The render fails before the save effect persists it.
