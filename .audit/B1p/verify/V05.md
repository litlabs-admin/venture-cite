# Verification batch V05

Findings examined: 10
CONFIRMED: 7   REFUTED: 3   UNCERTAIN: 0

## V-06 | REFUTED
Original claim: A later user can inherit the AI Visibility visited state after a reset-password sign-out.
What I checked: `client/src/pages/ai-visibility.tsx:828-841`, `client/src/lib/clientStorage.ts:13-36`, `client/src/pages/reset-password.tsx:107-135`, and `server/routes.ts:419-503`.
Verdict reasoning: The legacy key is written but no code reads it. The onboarding state comes from the user row through `/api/onboarding-status`. The reset path can leave a write-only key, but it cannot change another user's UI or data.

## V-01 | CONFIRMED
Original claim: A later user can inherit the selected AI engine through an unscoped local-storage key.
What I checked: `client/src/pages/ai-visibility.tsx:817-823`, `client/src/hooks/use-persisted-state.ts:8-37`, `client/src/lib/clientStorage.ts:13-36`, `client/src/hooks/use-auth.ts:34-41`, and `client/src/pages/reset-password.tsx:107-135`.
Verdict reasoning: `usePersistedState` reads and writes `vc_visibility_engine`. Cleanup removes only `venturecite-` keys and two different legacy keys. A successful normal logout leaves this key. The reset-password sign-out also omits cleanup.
Reachable path (CONFIRMED only): `client/src/pages/ai-visibility.tsx:820` -> `client/src/hooks/use-persisted-state.ts:14-16` -> User B receives User A's saved engine selection.
Corrected severity (CONFIRMED only): low, because the value changes a local interface preference only.

## F-S03-client-hooks-008 | CONFIRMED
Original claim: The New since last scan filter changes the URL but not the returned mention rows.
What I checked: `client/src/hooks/useMentions.ts:30-39`, `client/src/hooks/useMentions.ts:68-77`, `client/src/hooks/useMentions.ts:124-196`, `server/routes/mentions.ts:143-186`, `server/databaseStorage.ts:4946-5095`, and `tests/unit/useMentions.test.tsx:149-379`.
Verdict reasoning: The hook serializes `newSinceLastScan=true`. The list route does not read that parameter. The storage options and all three SQL branches omit this filter. The existing hook test does not exercise it.
Reachable path (CONFIRMED only): `client/src/components/geo-tools/MentionsFilters.tsx:238-250` -> `client/src/hooks/useMentions.ts:138-152` -> `client/src/hooks/useMentions.ts:181-190` -> `server/routes/mentions.ts:164-174` -> unfiltered rows.
Corrected severity (CONFIRMED only): medium, because the control returns incorrect results but does not change stored data.

## F-S03-client-hooks-009 | CONFIRMED
Original claim: The scan button stays disabled after the cooldown expiry without a parent render.
What I checked: `client/src/hooks/useMentions.ts:218-279`, `client/src/hooks/useMentions.ts:285-320`, `server/routes/mentions.ts:379-408`, `client/src/components/geo-tools/ScanStatusPanel.tsx:172-216`, and `tests/unit/useMentions.test.tsx:347-379`.
Verdict reasoning: A 429 response stores the cooldown time in hook state. The active-scan query stops polling when no scan exists. The panel timer rerenders only the panel. It does not recompute the hook memo, so `canStart` remains false.
Reachable path (CONFIRMED only): `server/routes/mentions.ts:396-403` -> `client/src/hooks/useMentions.ts:294-305` -> `client/src/hooks/useMentions.ts:273-279` -> `client/src/components/geo-tools/ScanStatusPanel.tsx:210-215` -> disabled button after expiry.
Corrected severity (CONFIRMED only): medium, because the user cannot start an allowed scan until another parent render occurs.

## F-S18-005 | REFUTED
Original claim: Cancellation can race with advancement and restore a cancelled workflow run to running.
What I checked: `server/lib/workflowEngine.ts:71-124`, `server/lib/workflowEngine.ts:153-159`, `server/lib/workflowEngine.ts:186-195`, `server/lib/workflowEngine.ts:615-645`, and `server/lib/workflowEngine.ts:647-686`.
Verdict reasoning: The race exists in isolation. Repository callers contain no call to `cancelRun`. The current application exposes no route or scheduler path that reaches it. I cannot construct the claimed cancellation path.

## F-S18-004 | REFUTED
Original claim: A crash after task creation can create duplicate paid workflow tasks on the next tick.
What I checked: `server/lib/workflowEngine.ts:126-150`, `server/lib/workflowEngine.ts:316-393`, `server/lib/workflowEngine.ts:647-686`, `server/databaseStorage.ts:4110-4147`, `shared/schema.ts:1765-1829`, and `migrations/0029_workflows.sql:3-37`.
Verdict reasoning: Task creation and run updates are separate. However, repository callers contain no call to `startRun`. The tick only advances existing active runs. The current application has no supported path that creates such a run.

## F-S03-001 | CONFIRMED
Original claim: The New since last scan button reports an active filter that the server ignores.
What I checked: `client/src/components/geo-tools/MentionsFilters.tsx:226-251`, `client/src/hooks/useMentions.ts:124-196`, `server/routes/mentions.ts:143-186`, `server/databaseStorage.ts:4946-5095`, and `tests/unit/mentionsRoutes.test.ts:247-254`.
Verdict reasoning: The button sets `newSinceLastScan` and shows it as active. The hook sends the parameter. The route and storage method do not accept it. No existing route test covers this parameter.
Reachable path (CONFIRMED only): `client/src/components/geo-tools/MentionsFilters.tsx:240` -> `client/src/hooks/useMentions.ts:181-190` -> `server/routes/mentions.ts:164-174` -> `server/databaseStorage.ts:5011-5074` -> rows from before the last scan remain.
Corrected severity (CONFIRMED only): medium, because this is the same incorrect filter behavior as F-S03-client-hooks-008.

## F-S03-002 | CONFIRMED
Original claim: A date-only To filter excludes mentions discovered later on that date.
What I checked: `client/src/components/geo-tools/MentionsFilters.tsx:161-180`, `client/src/hooks/useMentions.ts:181-190`, `server/routes/mentions.ts:164-174`, `server/databaseStorage.ts:5011-5074`, and `tests/unit/mentionsStorage.test.ts:457-523`.
Verdict reasoning: The date control sends `YYYY-MM-DD`. The route converts it to the start of that date. Each SQL branch uses an inclusive `discovered_at <= to` predicate. A same-day noon value is later than that boundary.
Reachable path (CONFIRMED only): `client/src/components/geo-tools/MentionsFilters.tsx:173-179` -> `client/src/hooks/useMentions.ts:181-190` -> `server/routes/mentions.ts:170-171` -> `server/databaseStorage.ts:5018` -> same-day afternoon rows are excluded.
Corrected severity (CONFIRMED only): medium, because the result set is wrong for a common date filter.

## F-S04-001 | CONFIRMED
Original claim: A probe source can supply a non-HTTP URL directly to an anchor element.
What I checked: `server/citationChecker.ts:224-248`, `server/lib/perceptionProbes.ts:240-264`, `server/routes/dashboard.ts:1692-1743`, `client/src/components/perception/ProbeMatrix.tsx:305-321`, and `tests/unit/probeMatrix.test.tsx:100-108`.
Verdict reasoning: Citation extraction accepts every string URL. The probe worker stores each value without validation. The read route returns the stored value without validation. The client assigns it to `href` without a scheme check. The existing test uses only an HTTPS value.
Reachable path (CONFIRMED only): Provider citation -> `server/citationChecker.ts:229-247` -> `server/lib/perceptionProbes.ts:257-264` -> `server/routes/dashboard.ts:1724-1737` -> `client/src/components/perception/ProbeMatrix.tsx:311-320` -> user click.
Corrected severity (CONFIRMED only): low, because exploitation requires a malicious or malformed provider citation and a user click.

## F-S04-002 | CONFIRMED
Original claim: An asked probe with no score displays as a red zero score.
What I checked: `shared/schema.ts:2508-2544`, `server/lib/perceptionProbes.ts:240-333`, `server/routes/dashboard.ts:1692-1743`, `client/src/components/perception/ProbeMatrix.tsx:61-116`, and `tests/unit/probeMatrix.test.tsx:43-143`.
Verdict reasoning: The schema permits `asked`. The worker writes `asked` before it runs scoring. The matrix handles only pending, failed, and no-information before the score branch. An asked row with a null score therefore renders `0.0`. The existing test lacks an asked case.
Reachable path (CONFIRMED only): `server/lib/perceptionProbes.ts:257-264` -> `server/routes/dashboard.ts:1709-1737` -> `client/src/components/perception/ProbeMatrix.tsx:74-114` -> red `0.0` while scoring remains incomplete.
Corrected severity (CONFIRMED only): medium, because the screen shows a false negative reputation score during a live run.
