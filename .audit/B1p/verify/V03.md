# Verification batch V03

Findings examined: 8
CONFIRMED: 7   REFUTED: 1   UNCERTAIN: 0

## F-S08-003 | REFUTED
Original claim: The Apply dialog can write optimized content to a newly selected article.
What I checked: `client/src/pages/geo-signals.tsx:1350-1373`, `client/src/pages/geo-signals.tsx:1779-1828`, `client/src/components/ui/dialog.tsx:9-52`, and `node_modules/@radix-ui/react-dialog/dist/index.mjs:30-36`.
Verdict reasoning: The dialog uses Radix `Dialog` with no `modal={false}` property. Radix defaults `modal` to true. The overlay blocks the article picker until the user closes the dialog. The claimed A to B selection path is unreachable.

## F-S08-006 | CONFIRMED
Original claim: An optimize response can enter the state slice for a newly selected article.
What I checked: `client/src/pages/geo-signals.tsx:307-328`, `client/src/pages/geo-signals.tsx:496-527`, `client/src/pages/geo-signals.tsx:1252-1282`, `client/src/pages/geo-signals.tsx:734-750`, `node_modules/@tanstack/react-query/src/useMutation.ts:38-40`, and `node_modules/@tanstack/query-core/src/mutationObserver.ts:89-93`.
Verdict reasoning: The picker remains enabled during optimization. A selection change rerenders the page. TanStack Query replaces pending mutation options after the render. The success callback then uses the new `articleKey`. The response content from article A enters article B's optimize slice.
Reachable path (CONFIRMED only): `ArticleSelect` at `client/src/pages/geo-signals.tsx:734-747` -> optimize click at `client/src/pages/geo-signals.tsx:1252-1264` -> selection change -> `onSuccess` at `client/src/pages/geo-signals.tsx:506-516` -> article B slice.
Corrected severity (CONFIRMED only): high. The result can later overwrite article B through the Apply action.

## F-S08-007 | CONFIRMED
Original claim: The schema audit can use the prior article's auto-filled URL.
What I checked: `client/src/pages/geo-signals.tsx:307-328`, `client/src/pages/geo-signals.tsx:426-441`, `client/src/pages/geo-signals.tsx:734-750`, and `client/src/pages/geo-signals.tsx:1433-1460`.
Verdict reasoning: Article selection changes `selectedArticleId`. The URL effect returns when `url` contains article A's URL. It does not reset `urlTouched` or `url`. The audit button submits that retained value after article B becomes selected.
Reachable path (CONFIRMED only): `ArticleSelect` at `client/src/pages/geo-signals.tsx:734-747` -> URL effect at `client/src/pages/geo-signals.tsx:426-441` -> selection change -> audit click at `client/src/pages/geo-signals.tsx:1457-1459` -> article A URL submitted for article B.
Corrected severity (CONFIRMED only): medium. The request audits the wrong page but does not change article data.

## F-S08-008 | CONFIRMED
Original claim: Two buttons are enabled for an article that has no content, but their handlers do nothing.
What I checked: `client/src/pages/geo-signals.tsx:263-304`, `client/src/pages/geo-signals.tsx:394-405`, `client/src/pages/geo-signals.tsx:695-708`, `client/src/pages/geo-signals.tsx:878-903`, and `client/src/pages/geo-signals.tsx:1048-1085`.
Verdict reasoning: The article query requests every status. The picker renders every returned article, including non-ready articles. Both handlers require content. Their disabled properties do not require content. A draft, generating, or failed article can enable both buttons and produce no feedback.
Reachable path (CONFIRMED only): article query at `client/src/pages/geo-signals.tsx:394-405` -> picker at `client/src/pages/geo-signals.tsx:281-301` -> button at `client/src/pages/geo-signals.tsx:878-881` or `client/src/pages/geo-signals.tsx:1048-1063` -> content guard returns.
Corrected severity (CONFIRMED only): low. The action does not send a request or change stored data.

## F-S02-server-jobs-002 | CONFIRMED
Original claim: Auto-citation work can run for a user who requested account deletion.
What I checked: `server/routes/userAccount.ts:212-220`, `shared/schema.ts:60-64`, `server/scheduler.ts:204-215`, `server/scheduler.ts:218-277`, and `server/routes/cron.ts:480-484`.
Verdict reasoning: Account deletion sets `users.deletedAt` and leaves the row for the grace period. The selector excludes deleted brands but not deleted users. A deleted user with a live paid brand enters the selector and receives citation work.
Reachable path (CONFIRMED only): account deletion at `server/routes/userAccount.ts:212-220` -> selector at `server/scheduler.ts:204-215` -> auto-citation loop at `server/scheduler.ts:228-277` -> `runBrandPrompts`.
Corrected severity (CONFIRMED only): high. The job spends paid provider work after the user requested deletion.

## F-S02-server-jobs-006 | CONFIRMED
Original claim: The weekly report can exceed its deadline and resend reports after a retry.
What I checked: `server/routes/cron.ts:8-14`, `server/routes/cron.ts:76-105`, `server/routes/cron.ts:155-200`, `server/routes/cron.ts:502-504`, `server/scheduler.ts:39-180`, `server/lib/jobDebounce.ts:94-109`, and `server/emailService.ts:220-253`.
Verdict reasoning: The orchestrator passes a deadline, but the weekly callback discards it. `Orchestrator.run` awaits the callback without a timeout race. The job processes users and brands in serial. The debounce stamp writes only after the whole callback returns. The user stamp does not filter future report eligibility. A timeout after an email leaves the job open for a retry that sends the same report again.
Reachable path (CONFIRMED only): Sunday orchestrator call at `server/routes/cron.ts:502-504` -> `Orchestrator.run` at `server/routes/cron.ts:168-200` -> `runWeeklyReportJob` at `server/scheduler.ts:39-56` -> serial user loop at `server/scheduler.ts:76-177` -> email send at `server/emailService.ts:226-237` -> missing completion stamp at `server/lib/jobDebounce.ts:107-109`.
Corrected severity (CONFIRMED only): high. The function can time out and send duplicate email during retry.

## F-S02-server-jobs-001 | CONFIRMED
Original claim: The citation selector loses `lastAutoCitationAt` when it casts raw SQL rows.
What I checked: `server/scheduler.ts:194-215`, `server/scheduler.ts:228-247`, `shared/schema.ts:312-313`, and `node_modules/drizzle-orm/node-postgres/session.js:102-114`.
Verdict reasoning: `SELECT b.*` returns PostgreSQL column names. Drizzle returns raw rows for `db.execute` when no selected fields exist. The cast at line 215 changes no properties. The loop reads `lastAutoCitationAt`, but the raw row has `last_auto_citation_at`. The due check therefore treats completed brands as never run.
Reachable path (CONFIRMED only): selector at `server/scheduler.ts:204-215` -> auto-citation loop at `server/scheduler.ts:228-247` -> due check at `server/scheduler.ts:194-199` -> run for an already completed brand.
Corrected severity (CONFIRMED only): high. The job can run expensive citation scans each hour instead of weekly.

## F-S21-002 | CONFIRMED
Original claim: Weekly reports and auto-citation can continue during the account-deletion grace period.
What I checked: `server/routes/userAccount.ts:212-220`, `shared/schema.ts:46-64`, `server/scheduler.ts:59-86`, `server/scheduler.ts:204-215`, `server/scheduler.ts:683-692`, and `server/scheduler.ts:742-748`.
Verdict reasoning: The deletion route leaves `weeklyReportEnabled` unchanged. The legacy weekly report selects users without a `deletedAt` filter. The auto-citation selector has the same omission. Other weekly jobs include `isNull(users.deletedAt)`, so the missing filter is not intended behavior.
Reachable path (CONFIRMED only): account deletion at `server/routes/userAccount.ts:212-220` -> report user query at `server/scheduler.ts:65-74` -> report email at `server/scheduler.ts:156-168`; or account deletion -> citation selector at `server/scheduler.ts:204-215` -> citation run at `server/scheduler.ts:274-277`.
Corrected severity (CONFIRMED only): high. The system contacts a user and spends paid work during the deletion grace period.

## Test evidence

I ran `npx.cmd vitest run --maxWorkers=1 tests/unit/citationCronUnconditional.test.ts tests/unit/autoCitationDeadline.test.ts tests/unit/jobDebounce.test.ts tests/unit/cronOrchestrator.test.ts`.

The command passed 31 tests in 4 files. These tests do not cover the eight claimed user paths. The selector test checks brand deletion and payment tier. It does not check `users.deleted_at` or raw-column mapping.
