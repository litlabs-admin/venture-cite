# Verification batch V10

Findings examined: 10
CONFIRMED: 8   REFUTED: 2   UNCERTAIN: 0

## F-S09-001 | CONFIRMED
Original claim: The fixed-width chart clips on a narrow viewport.
What I checked: client/src/pages/landing/sections/LearnResearch/AiSearchAreaChart.tsx:18-48; client/src/pages/landing/sections/LearnResearch/LearnResearch.tsx:40-44,86; no responsive chart test exists.
Verdict reasoning: The SVG has a fixed 1054px width. The bordered parent has `overflow-hidden`. The chart has no responsive width rule. A 390px viewport cannot show its full width.
Reachable path (CONFIRMED only): Landing page -> LearnResearch.tsx:86 -> AiSearchAreaChart.tsx:48 -> clipped SVG.
Corrected severity (CONFIRMED only): medium. This affects the public landing-page display. It does not affect user data or access control.

## F-S10-001 | CONFIRMED
Original claim: Stored citation URLs can reach an anchor without a protocol check.
What I checked: client/src/pages/prompt-detail.tsx:95-116,344-354; client/src/pages/prompt-diagnose.tsx:56-76,229-241; server/lib/urlExtractor.ts:13-51; server/lib/responseAnalyzer.ts:42-49,182-191; server/citationChecker.ts:758,899-904,1105-1128; server/lib/promptDiagnose.ts:212-227; tests/unit/urlExtractor.test.ts:34-38.
Verdict reasoning: The detail page uses `citedUrls`. The extractor only stores HTTP or HTTPS values. The diagnosis page also uses `citingOutletUrl`. The analyzer accepts each cited URL as any string. The citation writer prefers that value. The diagnosis anchor does not validate it.
Reachable path (CONFIRMED only): Citation response -> responseAnalyzer.ts:42-49 -> citationChecker.ts:902-904 -> geo_rankings -> promptDiagnose.ts:212-227 -> prompt-diagnose.tsx:234.
Corrected severity (CONFIRMED only): medium. The attacker needs a malicious provider response and a user click. The detail-page path has a protocol guard.

## F-S10-002 | CONFIRMED
Original claim: The account export omits prompt citation rankings.
What I checked: client/src/pages/settings.tsx:887-915,1093-1104; server/routes/userAccount.ts:86-146,266-294; shared/schema.ts:1089-1106; server/citationChecker.ts:1119-1134.
Verdict reasoning: The export selects `geoRankings` only when `articleId` belongs to an exported article. Prompt rows use `brandPromptId`. Their `articleId` can be null. The export includes the prompt records but omits their rankings.
Reachable path (CONFIRMED only): settings.tsx:891 -> userAccount.ts:267-290 -> userAccount.ts:123-131 -> export without prompt ranking rows.
Corrected severity (CONFIRMED only): medium. This creates an incomplete user export. It does not expose another user's data.

## F-S11-001 | CONFIRMED
Original claim: The migration assertion can disable TLS for a database connection.
What I checked: scripts/assertMigrationsApplied.mjs:16-52; package.json:37; .github/workflows/ci.yml:93-101.
Verdict reasoning: The script accepts `DATABASE_URL` when `TEST_DATABASE_URL` is absent. It passes `ssl: false` to `pg.Client`. The CI caller uses a local test database, but a manual production invocation with `DATABASE_URL` reaches the same client.
Reachable path (CONFIRMED only): npm run db:assert-migrations -> assertMigrationsApplied.mjs:29-41 -> pg.Client with TLS disabled.
Corrected severity (CONFIRMED only): low. The configured CI path uses loopback test PostgreSQL. The unsafe path needs a manual production invocation.

## F-S11-002 | CONFIRMED
Original claim: The bootstrap migration preflight does not verify that its direct target matches the runtime target.
What I checked: scripts/releaseEnvironmentPreflight.ts:258-275; scripts/migrationRelease.ts:44-76; scripts/migrate.ts:20-43; docs/deploy-runbook.md:105-113; tests/unit/migrationRelease.test.ts:87-123; server/lib/requestRoleMembership.ts:243-256.
Verdict reasoning: Bootstrap checks only the certificate path and the direct URL. The test proves that bootstrap accepts no `DATABASE_URL`. `migrate.ts` then replaces `DATABASE_URL` with the direct URL. The identity comparison exists only in the separate role-membership command.
Reachable path (CONFIRMED only): npm run db:migrate:bootstrap -> migrationRelease.ts:48-50 -> releaseEnvironmentPreflight.ts:261-274 -> migrate.ts:31-36 -> migration runner target.
Corrected severity (CONFIRMED only): medium. An operator can migrate a valid but wrong database. The command still needs explicit production confirmation.

## F-S12-003 | REFUTED
Original claim: Every normal citation check makes an unrecorded citation-judge request.
What I checked: server/citationChecker.ts:277-396,437-443,663-688,724-758; server/citationJudge.ts:58-121; server/routes/prompts.ts:873-916; tests/unit/citationChecker.matcherAuthority.test.ts:43-132.
Verdict reasoning: The normal `runBrandPrompts` worker passes `skipJudge: true` at citationChecker.ts:679-688. It uses the merged analyzer instead. The cited judge call at citationChecker.ts:394-396 is not on that normal path. The separate phrasing route can call the judge, but it does not prove the stated normal-path claim.

## F-S12-006 | CONFIRMED
Original claim: A slice deadline can mark a resumable job as failed.
What I checked: server/contentGenerationWorker.ts:81-95,105-203,334-435; server/routes/content.ts:435-489; server/routes/cron.ts:211-226; tests/unit/contentGenerationResponses.test.ts:99-345.
Verdict reasoning: `providerRequestOptions` throws `TimeoutError` after the deadline. `runArticleSlice` catches every exception and calls `failContentJobSlice`. Only a returned `kind: "deadline"` releases the lease. The advance route uses an eight-second deadline.
Reachable path (CONFIRMED only): content.ts:445-472 -> contentGenerationWorker.ts:85-89 -> contentGenerationWorker.ts:358-364 -> failContentJobSlice.
Corrected severity (CONFIRMED only): medium. A delayed provider or database call can fail one generation job. The failure does not corrupt other jobs.

## F-S12-007 | CONFIRMED
Original claim: A job can write Brand A content into an article moved to Brand B.
What I checked: server/routes/articles.ts:62-89,194-235; server/data/contentRequestArticleRepository.ts:58-81,203-237; migrations/0104_content_request_article_writes.sql:12-60; migrations/0113_rls_current_setting_initplan.sql:92-111; server/databaseStorage.ts:1623-1691.
Verdict reasoning: The update schema and repository accept `brandId`. The RLS policy permits a move between two brands of the same user. Completion checks article ID and job ID, but it does not compare `articles.brandId` with `content_generation_jobs.brandId`.
Reachable path (CONFIRMED only): articles.ts:195-231 -> contentRequestArticleRepository.ts:203-237 -> article brand move -> databaseStorage.ts:1655-1669 -> content write.
Corrected severity (CONFIRMED only): medium. The RLS policy prevents a cross-user move. The defect mixes data between two brands of one user.

## F-S14-001 | CONFIRMED
Original claim: The AI logger writes unredacted request and response data through `console`.
What I checked: server/lib/aiLogger.ts:3-50; server/citationChecker.ts:24-45; server/citationJudge.ts:5-13; server/lib/contentGenerationProvider.ts:151-158.
Verdict reasoning: The wrapper writes the complete `messages` object and response text with `console.log`. It writes error text with `console.error`. Truncation limits length but does not remove private values. Several production AI clients attach the wrapper.
Reachable path (CONFIRMED only): citationChecker.ts:26-31 -> aiLogger.ts:24-42 -> stdout request and response data.
Corrected severity (CONFIRMED only): high. Prompts and model output can contain brand facts and user data. The logger writes them to external runtime logs.

## F-S14-002 | REFUTED
Original claim: Concurrent chatbot requests can exceed an active token or message limit through a non-atomic check.
What I checked: server/lib/chatbotBudget.ts:10-63; server/lib/llmPricing.ts:8-41; server/routes/assistant.ts:208-210,375-388; tests/unit/chatbotBudget.test.ts:3-80.
Verdict reasoning: Every current chatbot token cap is `-1`. `assertChatbotBudget` returns at chatbotBudget.ts:38 before it reads tokens or messages. Therefore no active token or hourly message limit has the claimed concurrent check-and-record path. The test states and exercises this production configuration.
