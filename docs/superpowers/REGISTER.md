# Backend Remediation — Register

The durable record. Nothing here is deleted; items change **status** only.

Scratch lives in `.superpowers/sdd/backend-remediation/` (gitignored: briefs, diffs,
JSON reports, the append-only ledger). This file is tracked, so it survives a
`git clean`, a machine reset, and a context compaction.

- **Branch:** `backend-remediation-2026-08-27` (off `main` @ `9aafd48`)
- **Baseline:** `npm test` = **0 failed / 1604 passed / 91 skipped**. The extra skip is the
  new real-Postgres integration file, which skips without `TEST_DATABASE_URL`.
  Previously 1 failed; the cause was D-4 (line endings), now fixed. **Any failure from here
  is a real regression** — there is no longer a documented-failure allowance.
- **Plans:** [remediation](plans/2026-08-27-backend-remediation.md) ·
  [orchestration](plans/2026-08-27-codex-orchestration.md) ·
  [parallel execution](plans/2026-08-27-parallel-execution.md)
- **Agent rules:** [AGENT-CONTRACT.md](AGENT-CONTRACT.md)

---

## 1. Task state

| #      | Task                                           | Lane            | Model      | Status                                                                                                                            |
| ------ | ---------------------------------------------- | --------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| **0**  | **`.gitattributes` — pin text files to LF**    | prerequisite    | controller | **DONE & VERIFIED** — 570 working-tree files normalised, 0 content changes, suite now 0 failed                                    |
| 1      | `RETURNING` round trips                        | A               | luna/high  | **COMPLETE** — F-01…F-04b fixed, all controller-verified                                                                          |
| **2a** | **Migration runner: `no-transaction` pragma**  | **B, before 2** | terra/max  | **COMMITTED** — `1c90f2d`, pragma verified                                                                                        |
| 2      | Composite indexes (`0117`)                     | B               | luna/low   | **MERGED & VERIFIED** — 5 indexes, mirror regenerated, all gates green                                                            |
| 3      | Batch competitor insert                        | A               | luna/high  | **MERGED & VERIFIED** — batched, per-row fallback preserved, `IStorage` corrected                                                 |
| 4      | Metrics aggregate → SQL                        | A               | terra/high | **MERGED & VERIFIED** — SQL aggregate, grain preserved, float8 cast proven load-bearing                                           |
| 5      | Citation trend → SQL                           | A               | terra/high | **MERGED & VERIFIED** — date_trunc equivalence verified, zero-fill retained                                                       |
| 6      | Gap-matrix N+1                                 | A               | terra/high | **MERGED & VERIFIED** — one query for six competitors, degrade-to-empty preserved                                                 |
| 7      | Advisory locks → xact scope                    | D               | terra/max  | not started                                                                                                                       |
| 8      | Retention + FK indexes + RLS policies (`0118`) | C               | luna/high  | **MERGED & VERIFIED** — api_costs prune, 3 FK indexes, 2 RLS initplans (verified on real Postgres)                                |
| **8b** | **CHECK constraints on 4 status columns**      | C               | terra/max  | **RESOLVED — recommend against.** Value set unprovable by static analysis; three sources disagree. Observe-then-constrain instead |
| 9      | Runtime-role staging procedure                 | E               | terra/max  | **RESOLVED — cutover not viable (F-55)**                                                                                          |

---

## 2. Corrections to my own earlier claims

Each was published before it was checked. Listed so no downstream reader inherits them.

| ID   | Claim I made                                                                                                                                                                                       | Reality                                                                                                                                                                                                                                                                                                                                                                                                                   | How it was caught                                                                                                          |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| C-01 | `/api/geo-analytics/*` has a cross-tenant read/write hole                                                                                                                                          | False. `enforceBrandOwnership` (`routes.ts:124`) and `app.param("brandId")` (`:129`) are global middleware registered **before** every domain route                                                                                                                                                                                                                                                                       | An agent wrote an actual Express 5 test instead of reading handlers in isolation                                           |
| C-02 | `safeFetchTextWithLockedIp` pins the connection to a resolved IP, so scraping can never leave Node                                                                                                 | False. Its own comment records that pinning was tried twice and abandoned; it re-validates DNS per redirect hop then calls plain `fetch`                                                                                                                                                                                                                                                                                  | Read the function body instead of trusting its name                                                                        |
| C-03 | `pg_timezone_names` (216 calls, 96s) is the profile-update validation                                                                                                                              | False. `userAccount.ts:337-348` validates via `Intl.supportedValuesOf` and never touches the DB. That traffic is Studio/tooling                                                                                                                                                                                                                                                                                           | Grepped for the table name and found zero app matches                                                                      |
| C-04 | There is no `gpt-5.6-terra`                                                                                                                                                                        | False. It was absent from this repo and both configs, but it is live on the account and returns exit 0                                                                                                                                                                                                                                                                                                                    | Probed the model directly                                                                                                  |
| C-05 | Edge Functions cap at ~150s, so six jobs can never move                                                                                                                                            | Wall clock is 400s on paid plans; `EdgeRuntime.waitUntil` exists; npm + Node built-ins are GA. Six of eight "blockers" fell                                                                                                                                                                                                                                                                                               | Read the current limits docs                                                                                               |
| C-06 | Next migration number is `0125`/`0126`                                                                                                                                                             | Highest existing is `0116`. Corrected to `0117`/`0118`                                                                                                                                                                                                                                                                                                                                                                    | Listed `migrations/` while planning parallel lanes                                                                         |
| C-08 | Plan Global Constraint: "CI runs `--check`" on the migration mirror                                                                                                                                | **False.** `.github/workflows/*.yml` never invokes `supabase:migrations:check`. That is exactly why the mirror drifted 9 files without anyone noticing                                                                                                                                                                                                                                                                    | Grepped the workflows                                                                                                      |
| C-09 | The unpinned fact-scrape lock (`databaseStorage.ts:3846-3859`) is "the worst broken lock site… acquire and release as two independent pool round trips with a full LLM-driven scrape between them" | **The methods had zero callers.** Only their own definitions and their unit test referenced them; the real scrape path already used `withDynamicAdvisoryLock`, which pins one client. The defect was real but **unreachable**, so it had no production impact — and the 64-second `pg_advisory_lock` block seen in production therefore came from `migrationRunner`, the only _blocking_ lock, not from here              | `git grep` for callers at the pre-task commit                                                                              |
| C-10 | D-5's claim that the long-job lock sites "already pin a client, so the session lock is not the bug there"                                                                                          | **False.** Measured on the production pooler: 8 concurrent pinned clients, 6 statements each with idle gaps — **7 of 8 rotated across backends mid-session**. Supavisor multiplexes client connections onto fewer backends and reassigns per transaction, so pinning preserves nothing. D-5's _conclusion_ (don't xact-scope long jobs) still stands; its premise did not                                                 | Direct measurement against the production pooler                                                                           |
| C-11 | F-20 (four orphaned `public.users` rows) blocks Task 9                                                                                                                                             | **False.** The existing policies key on the `venturecite.user_id` GUC, not `auth.uid()`. Orphaned auth rows block a future PostgREST/`auth.uid()` adoption, which is separate and unscheduled                                                                                                                                                                                                                             | Read the live policy definitions                                                                                           |
| F-55 | **High**                                                                                                                                                                                           | **Task 9's cutover is not viable as planned.** Enumerated by creating `venturecite_runtime` locally and probing: only **12 of 72 tables** carry any grant to the restricted roles; **60 have none**, and the covered 12 are narrow (`brand_prompts` has SELECT on one column). Granting only what is needed breaks the app; granting enough to work reproduces owner-equivalent access under another name                 | **RESOLVED — grow the restricted path instead of flipping the connection.** See `plans/2026-08-27-runtime-role-cutover.md` |
| P-4a | Evidence                                                                                                                                                                                           | **Session advisory locks are unsafe on this connection path, measured.** One idle pinned client holds a single backend — which is why the bug was invisible — but under 8-way concurrency 7 of 8 clients rotated. `pg_advisory_unlock` can therefore land on a different backend than the acquire, orphaning the lock. This is the most plausible mechanism for the 64,164 ms `pg_advisory_lock` block seen in production | **VERIFIED**                                                                                                               |
| F-54 | Info                                                                                                                                                                                               | A lease has a failure mode advisory locks do not: under event-loop starvation, renewal can miss and the lease expire while the job still runs, admitting a second holder. TTL 60s with renewal every ~20s requires three consecutive misses. Accepted as the better trade given P-4a, but worth knowing                                                                                                                   | **ACCEPTED — documented**                                                                                                  |
| C-07 | Task 1: both ranking methods do two round trips                                                                                                                                                    | Only `createCompetitorGeoRanking` did. `createGeoRanking` was already one trip on the happy path — see R-04                                                                                                                                                                                                                                                                                                               | Reviewing the worker's diff                                                                                                |

---

## 3. Rulings

Decisions taken on the user's behalf. Each carries what it costs if wrong.

| ID   | Ruling                                                                                                                                     | Cost if wrong                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| R-01 | Task 1 uses Drizzle's insert builder, not raw SQL + `RETURNING *`. `db.execute` returns snake_case; the method contract is typed camelCase | One method rewritten; caught by `npm run check`                                |
| R-02 | Task 1 effort raised low → high. Type mapping makes it judgement, not transcription                                                        | Slightly more spend on one task                                                |
| R-03 | No `makeStorage()` DI seam. Existing tests mock `../../server/db` directly (14+ precedents)                                                | Test sits at a different seam than the plan drafted                            |
| R-04 | **Revert `createGeoRanking` entirely.** The plan misdiagnosed it (C-07); the "fix" introduced F-01 and F-02 for no gain                    | Forgo a micro-optimisation on a deliberately rare conflict path                |
| R-05 | Global Codex default model changed `gpt-5.6-sol` → `gpt-5.6-luna`, so a dispatch missing `-m` fails safe                                   | An unflagged dispatch runs cheaper than intended rather than on a banned model |
| R-06 | `spark_worker` repointed off `gpt-5.3-codex-spark` (Pro-only text-only preview) onto luna/low                                              | Marginally slower on trivial edits                                             |
| R-07 | Buffer MCP disabled in `.codex/config.toml` — its dead token made correct runs exit 255                                                    | Buffer tooling unavailable to agents; no task needs it                         |
| R-08 | Durable register lives in tracked `docs/`; only scratch in gitignored `.superpowers/`                                                      | Slightly more in version control                                               |

| R-09 | **Split CHECK constraints out of Task 8 into a separate Task 8b.** A CHECK narrower than the value set the code can write does not fail in review — it fails in production on the first legitimate state transition. The live data already contradicts the schema comment (`brand_fact_scrape_runs` holds `completed`, absent from its documented `pending\|planning\|fetching\|extracting\|slice_pending` list), so the comments cannot be used as the source. Task 8 keeps retention, FK indexes and the two RLS policy fixes — all low-risk | Deferring a Medium-severity guard; typos in status values stay uncaught until 8b lands |
---

## 4. Findings register

Status: **OPEN** · **FIXING** · **FIXED** · **DEFERRED** · **PARKED**

### From Task 1 review (terra/high)

| ID    | Sev                     | Finding                                                                                                                                                                                | Status                                                                                                                                                                                                                            |
| ----- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-01  | Important               | `createGeoRanking` targeted conflict lets a PK conflict throw                                                                                                                          | **FIXED** — verified `createGeoRanking` is byte-identical to `main`                                                                                                                                                               |
| F-02  | Important               | `DO UPDATE SET id = id` writes a row version and takes a lock per duplicate                                                                                                            | **FIXED** — same revert, independently verified                                                                                                                                                                                   |
| F-03  | ~~Minor~~ **Important** | Test proves call sequence only — db is mocked, so the COALESCE fragments and conflict target on the hottest write path are never executed                                              | **CLOSED** — `tests/integration/competitorGeoRankingUpsert.test.ts` executes the real SQL on local Postgres (5 tests). Mutation-tested: breaking `COALESCE(EXCLUDED.rank, …)` fails with `expected null to be 3`; restored, green |
| F-04  | Minor                   | `databaseStorage.ts` Prettier regression                                                                                                                                               | **FIXED** — verified clean                                                                                                                                                                                                        |
| F-04b | Minor                   | The task's own new test file was Prettier-dirty, reported again as "267 existing files"                                                                                                | **FIXED** — controller ran `prettier --write`. Deliberate exception to the no-controller-fixes rule: a formatting-only change on one file did not justify a dispatch round. Recorded rather than disguised                        |
| F-05  | Process                 | Two fix dispatches used a bare fix brief without attaching `AGENT-CONTRACT.md`, so §5 (state concerns at the right scope) never reached the worker. Controller error, not worker error | **FIXED** — `scripts/dispatch-agent.ps1` attaches the contract on every dispatch, including fix rounds                                                                                                                            |

### From the backend audit — not yet scheduled

| ID    | Sev      | Finding                                                                                                                                                                                                                           | Status                                                                                                                                                          |
| ----- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-10  | Critical | App connects as `postgres`; migrations 0096–0114's three roles, grants and 26 policies enforce nothing outside four route files                                                                                                   | Task 9 — **OPEN**                                                                                                                                               |
| F-11  | Critical | 994 `anon`/`authenticated` table grants; deny-all RLS is the only barrier                                                                                                                                                         | **UNBLOCKED** — was deferred behind Task 9; that assumption is void (F-55)                                                                                      |
| F-12  | Medium   | Account deletion never calls `admin.signOut` — token stays valid to its TTL                                                                                                                                                       | **OPEN**, unscheduled                                                                                                                                           |
| F-13  | Medium   | `api_costs`, `geo_rankings`, `competitor_geo_rankings`, `competitor_citation_snapshots` have no retention policy                                                                                                                  | **Task 8 — scope cut to `api_costs` only** (D-6)                                                                                                                |
| F-13b | Medium   | Retention policy for `geo_rankings`, `competitor_geo_rankings`, `competitor_citation_snapshots` — all unbounded, all product data. Needs a **product decision** on how much citation history to keep, not a default 90-day window | **OPEN — needs owner input**                                                                                                                                    |
| F-14  | Medium   | Two RLS policies still re-evaluate `current_setting()` per row                                                                                                                                                                    | Task 8 — **OPEN**                                                                                                                                               |
| F-15  | Medium   | Four status columns gating invariants have no CHECK constraint                                                                                                                                                                    | **OPEN** — moved to Task 8b; needs per-column value-set derivation from every write site, not from schema comments                                              |
| F-16  | Low      | `mentions.ts` is the only `express.Router()`; app-level `param` guards don't propagate into it                                                                                                                                    | **OPEN** — footgun for the next sub-router, not a live hole                                                                                                     |
| F-17  | Low      | `factScrapeBackstop.ts:58-73` xact lock doesn't cover `runAggregate`'s own transaction                                                                                                                                            | Task 7 — **OPEN**                                                                                                                                               |
| F-18  | Low      | Two lock mechanisms guard the same resource with different keys                                                                                                                                                                   | Task 7 — **OPEN**                                                                                                                                               |
| F-19  | Info     | `PUT /api/board/:boardId` is deliberately public and unauthenticated                                                                                                                                                              | **PARKED** — by design                                                                                                                                          |
| F-20  | Medium   | 4 of 47 `public.users` rows have no matching `auth.users` row; any `auth.uid()` policy locks them out                                                                                                                             | **TRIAGED** — 3 test artifacts (`smoke-user` owns 6 brands, non-UUID id: leaked fixture), 1 apparent real signup with no auth row. Cleanup is an owner decision |
| F-21  | Low      | Both schedulers enabled at once (in-process node-cron + hourly GitHub Actions)                                                                                                                                                    | **OPEN**                                                                                                                                                        |
| F-22  | Medium   | Suite baseline not green — `core.autocrlf=true` with no `.gitattributes`                                                                                                                                                          | **FIXED** — Task 0. Suite now 0 failed                                                                                                                          |

### Infrastructure — found while standing up local verification

| ID   | Sev      | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                                                                                                                                                                                                           |
| ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-40 | **High** | **The migration mirror was 9 files stale and the repo's own gate was red on `main`.** `migrations/` had 124 files (latest `0116`); `supabase/migrations/` had 116 (latest `0114`) and was _also_ missing 0094–0100 from the middle. `npm run supabase:migrations:check` failed outright. Consequence: any `supabase start`, `supabase db reset`, or Supabase preview branch built an **incomplete schema** — 62 of 74 tables, missing exactly the tables those 9 migrations create                                                                                                                                         | **FIXED** — `supabase:migrations:sync` regenerated the 9 files; check now exits 0                                                                                                                                |
| F-41 | **High** | **CI never runs `supabase:migrations:check`**, which is why F-40 drifted silently. The mirror can rot indefinitely with no signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **FIXED** — added as a `Supabase migration mirror` step in `.github/workflows/ci.yml`, after Type check                                                                                                          |
| F-43 | **High** | **Seven pairs of duplicate migration numbers: 0094–0100.** Fourteen files share seven numbers (e.g. `0094_site_health_scan_history` + `0094_stripe_webhook_processing_claim`), almost certainly two branches numbering in parallel and both merging. **Production is unaffected** — `migrationRunner` keys its ledger on _filename_. **All Supabase tooling is broken** — it keys on _version_, so `supabase db reset` / `start` / every preview branch dies on `duplicate key value violates unique constraint "schema_migrations_pkey"`. This is the true root cause of F-40: the sync script emitted colliding versions | **FIXED & VERIFIED** — sync derives versions from sorted ordinal position; mirror regenerated (125 files, 0 duplicate versions); `supabase db reset` applies all 124 migrations; local schema matches production |
| F-44 | Medium   | `syncSupabaseMigrations.mjs` has **no duplicate-version detection**, so `supabase:migrations:check` exits 0 while `supabase db reset` fails on the artifact it just validated                                                                                                                                                                                                                                                                                                                                                                                                                                              | **FIXED & NEGATIVE-TESTED** — `assertUniqueDestinationVersions` added; reintroducing the collision fails naming both files, restoring exits 0                                                                    |
| F-45 | Info     | Production's `supabase_migrations.schema_migrations` holds **1 row** (a snapshot, `20260821211044`), not per-file versions. The real production record is `public.schema_migrations` (122 rows), filename-keyed. **Therefore renumbering mirror versions is production-safe**                                                                                                                                                                                                                                                                                                                                              | **VERIFIED** — this is the safety argument for fixing F-43                                                                                                                                                       |
| F-42 | Medium   | `supabase start` **exits 0 while failing**. It reported `LegacyDockerLifecycleInspectError` (Docker engine down) and still returned exit code 0, which briefly led me to report the stack as up when it was not                                                                                                                                                                                                                                                                                                                                                                                                            | **OPEN** — never trust exit code alone from the `supabase` CLI; confirm with `supabase status`                                                                                                                   |

| F-47 | **Important** | **Regression I caused.** Ordinal renumbering (F-43) broke 10 tests: `requestRlsMigrationShape` and `rlsInitplanMigrationShape` hardcoded 12 generated mirror filenames (`supabase/migrations/20260421000112_0112_…`). Caught immediately by the now-green baseline | **FIXED** — added `tests/helpers/supabaseMirror.ts`, which resolves a mirror file from its ROOT filename and throws on missing/ambiguous matches. Tests are now decoupled from the generated version scheme permanently, which is the correct design regardless of this change |

| F-48 | Medium | **Flaky test: `tests/unit/tourCompletionPersists.test.ts`.** Failed once inside a full-suite run and passed on focused rerun. Cause is wall-clock dependence: a poll loop waits for `.shepherd-content` against a hard 3000 ms deadline (`:386`), with `setTimeout` flushes at `:63` and `:88`. Under full-suite CPU contention the deadline can lapse. Pre-existing — not caused by Task 2a | **OPEN** — undermines the green-baseline contract, since an intermittent red is indistinguishable from a real regression. Needs a deterministic wait (fake timers or an event-driven condition), not a longer timeout |

| F-49 | Process | **Brief defect, mine.** Task 2's brief told the worker to run `supabase:migrations:sync` AND to change only two named files. Sync generates a third file (the mirror). The worker correctly obeyed the stricter rule and deleted the generated artifact, leaving `supabase:migrations:check` red. Briefs that name an exact file set must exempt generated artifacts, or exclude the sync step and leave it to the controller | **FIXED** — mirror regenerated centrally after merge; check exits 0. Future briefs: the controller runs sync at merge time |
| F-50 | Medium | **Shared `node_modules` junction causes concurrent-write contention across worktrees.** Lane B hit `EPERM` writing `node_modules/typescript/tsbuildinfo` and again on Vitest cache creation, so `npm run check` and `npm test` could not complete in-lane. The work itself was fine | **OPEN — mitigated.** The controller verifies centrally after merge rather than trusting in-lane runs. A real fix is per-lane `node_modules`, at the cost of an `npm ci` per worktree |

| F-52 | Process | **Brief defect repeated (mine).** The 4+5+6 brief excluded `server/storage.ts` while requiring four new storage methods; `storage` is typed as `IStorage`, so nothing could compile. Terra returned `NEEDS_CONTEXT` and refused to proceed — correct per the contract. Notable contrast: given the identical constraint on Task 3, Luna worked around it with a structural cast, Terra escalated | **FIXED** — brief amended to include `storage.ts` and to forbid the cast explicitly |
| F-53 | Process | **I deleted the workspace.** Junctioning the shared `node_modules` into each lane worktree, then running `git worktree remove --force`, followed the junctions and deleted the real `node_modules` (641 packages). No source lost; `npm ci` restored it. Verified afterwards that `rmdir` on a junction unlinks safely while leaving the target intact | **FIXED** — correct order is unlink first, then remove the worktree. Better: per-lane `npm ci` |
| P-1a | Evidence | **Task 2's composite index is justified, measured on production.** Today's plan for the hot aggregate uses the single-column `geo_rankings_brand_prompt_id_idx` and applies `checked_at` as a **Filter**, reporting `Rows Removed by Filter: 16` per loop across 85 loops — roughly 23 rows fetched to keep 7. The composite `(brand_prompt_id, checked_at desc)` turns that Filter into an Index Cond, removing ~70% of the heap fetches on that path. Production execution time for this shape: **537 ms** | **VERIFIED** — read-only `EXPLAIN ANALYZE` on production |
| P-2a | Evidence | **The SQL aggregate returns 15 rows where the JS path returned 4,950** for the same filter at production scale — a ~330x reduction in rows shipped to Node. Measured locally against 21,600 seeded rows | **VERIFIED** |

| P-3a | Evidence | **The `::float8` cast is load-bearing, proven on real Postgres.** `avg(relevance_score)` on an `integer` column returns `numeric`, which node-postgres yields as a **string**: `typeof` is `"string"` and `.toFixed(2)` throws `n.toFixed is not a function`. With the cast the driver yields a `number` and `.toFixed(2)` returns `68.62`. Without it, `recordCurrentMetrics` would throw after **every citation run** — and the mocked unit tests could never have caught it | **VERIFIED** |

### Tooling

| ID   | Finding                                                                                       | Status                                                                  |
| ---- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| F-30 | `~/.codex/config.toml` `service_tier = "default"` invalid                                     | **FIXED** — commented out                                               |
| F-31 | `.codex/config.toml` flat `[agents]` block rejected by CLI schema                             | **FIXED** — removed, per-agent files retained                           |
| F-32 | codex-cli 0.130.0-alpha.5 could not parse the `max` reasoning level                           | **FIXED** — `npm i -g @openai/codex@latest` → 0.150.0                   |
| F-33 | Dead Buffer MCP token made correct runs exit 255                                              | **FIXED** — R-07                                                        |
| F-34 | `xhigh` and `ultra` hard-error on this account; `deep-worker`/`docs-worker` specified `xhigh` | **FIXED** — both now `max`                                              |
| F-35 | `-p/--profile` silently ignores unknown names; does not read `.codex/agents/`                 | **FIXED** by policy — every dispatch passes `-m`, `-c`, `-s` explicitly |
| F-36 | `--output-schema` requires `required` to list **every** property; `concerns` was optional     | **FIXED** — schema corrected                                            |
| F-37 | Two Codex installs: npm 0.150.0 (on PATH, working) and desktop-app 0.130.0-alpha.5            | **OPEN** — drop the npm copy if the desktop app passes 0.150            |

---

## 5. Deferred by decision

Recorded so they are not silently forgotten.

| Item                                           | Why deferred                                                                                                                        |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Migrate 57 `varchar` UUID PKs to native `uuid` | 81 FK columns and an app-wide rewrite to save single-digit MB. Keep `uuid` for new tables; cast `auth.uid()::text` in policies      |
| Drop ~30 unused indexes                        | 2.2 MB of 48 MB, mostly on sub-1,000-row tables for features that haven't ramped. Re-adding costs a lock that scales with the table |
| Partition `competitor_geo_rankings`            | 72,209 rows — three orders of magnitude below where partitioning pays                                                               |
| Nine of twelve flagged unindexed FKs           | Columns are never queried; tables under 1,000 rows. Only 3 earn an index                                                            |

---

## 6. Proof obligations

Work is not done until these are re-measured **by the controller**, not claimed by a worker.

| #   | Obligation                                 | Baseline                            | When                |
| --- | ------------------------------------------ | ----------------------------------- | ------------------- |
| P-1 | Top query row count falls                  | 2,117,407 rows / 420.8s / 980 calls | After Lane A merges |
| P-2 | Total rows shipped to JS falls             | ~8.2M from tables holding 93,612    | After Lane A merges |
| P-3 | Single-row inserts fall                    | 84,892 inserts, 2 round trips each  | After Task 3        |
| P-4 | Advisory lock max block falls              | 64,164 ms                           | After Task 7        |
| P-5 | Unindexed-FK + RLS-initplan advisors clear | 12 FKs, 2 initplan warnings         | After Tasks 2 and 8 |
| P-6 | No new test failures                       | 1 failed / 1602 passed / 90 skipped | After every merge   |

---

## 7. Plan defects found by verification

Found by checking the plan against the running system instead of trusting my own prose.
Each would have shipped broken.

| ID       | Defect                                                                                                                                                                                                                                                                                                                                                                                                           | Evidence                                           | Resolution                                                                                                                                                                                                                                                                                                                               |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-1**  | Task 2's `CREATE INDEX CONCURRENTLY` **cannot execute**. `migrationRunner.ts:239-246` wraps every migration file in `BEGIN`…`COMMIT`, and Postgres forbids `CONCURRENTLY` inside a transaction block. The plan listed "must be CONCURRENTLY" as a Global Constraint while making it impossible.                                                                                                                  | Read `migrationRunner.ts:230-265`                  | **New Task 2a**: add a `-- migrate:no-transaction` pragma to the runner, then Task 2 uses `CONCURRENTLY` legitimately. Rejected alternatives: plain `CREATE INDEX` (routes around the gap, and these tables are written by live citation runs); out-of-band application (drifts from the ledger and breaks `supabase:migrations:check`). |
| **D-2**  | Task 4's SQL is **incomplete**. `metricsSnapshot.ts:74-88` writes `metricDetails: { cited, scored }`, where `scored` is the count of cited rows with a **non-null** `relevance_score`, and only writes the snapshot at all when `scored > 0`. My aggregate returned `checks`, `cited`, `avg_relevance` — no `scored`. Implementing it as written would silently drop a field and change when the row is written. | Read `metricsSnapshot.ts:70-88`                    | Revise the aggregate to return `count(relevance_score) filter (where is_cited = 1) as scored`, and preserve the `scored > 0` guard.                                                                                                                                                                                                      |
| **D-2b** | `relevance_score` is `integer`, so `avg()` returns `numeric`, which node-postgres returns as a **string**. The JS path produces `avgRelevance.toFixed(2)`. A naive port would compare or store a string where a number is expected.                                                                                                                                                                              | `information_schema.columns` on the live DB        | Cast explicitly in SQL and format to 2dp, matching `toFixed(2)` exactly.                                                                                                                                                                                                                                                                 |
| **D-2c** | Task 4's SQL grouped by `(brand_prompt_id, ai_platform)`, but `byPromptMap` in `metricsSnapshot.ts:50-61` groups by `brandPromptId` **only**. Implementing as written would have changed the shape of `metricDetails.byPrompt`, which `weeklyCatchup`'s `delta_calc` step diffs run-over-run                                                                                                                     | Read `metricsSnapshot.ts:37-88` line by line       | Task 4 spec rewritten with the correct grain, plus the null-`brandPromptId` skip and the `scored > 0` write guard called out explicitly                                                                                                                                                                                                  |
| **D-3**  | Task 1's change is **unverified against a real Postgres**. The test mocks the db, so the `COALESCE` fragments and the partial-index conflict target are never executed. This is the hottest write path in the system (84,892 inserts). Writing a comment that says "SQL correctness is not covered" is documentation of a gap, not closure of it.                                                                | Terra review F-03; confirmed by reading the test   | Docker 29.5.3 and Supabase CLI 2.116.0 are both installed, so a **real local Postgres integration test** is achievable via the existing `TEST_DATABASE_URL` / `LOCAL_SUPABASE_TEST=1` gate. Close the gap rather than annotate it.                                                                                                       |
| **D-4**  | The "pre-existing baseline failure" is **not a code defect** — it is a repo-wide line-ending defect. `core.autocrlf=true` with **no `.gitattributes`**. `migrations/0112_*.sql` has 68 CRLF pairs and 0 bare LF; the test's needle is absent with `\n` and present after CRLF normalisation. Passes on Linux CI, fails on this Windows checkout.                                                                 | `node` byte-level read; `git config core.autocrlf` | **New Task 0**, a prerequisite: add `.gitattributes` pinning text files to LF. Also removes the spurious `LF will be replaced by CRLF` churn on every agent write, which would otherwise make worktree merges conflict catastrophically.                                                                                                 |

| **D-5** | **Task 7 as specified would have caused an outage.** It said "convert every advisory lock to transaction scope". But `withAdvisoryLock` wraps entire cron jobs — `scheduler.ts:222` auto-citation (observed 25+ min, ~180 LLM calls), `:427` mention-scan, `:463` listicle-scan, `:538`/`:605`/`:666` purge and weekly-report. `pg_advisory_xact_lock` would hold an open transaction and a pooled backend for the whole job: idle-in-transaction for minutes, VACUUM blocked, and instant death to any `idle_in_transaction_session_timeout`. These sites already pin a client via `pool.connect()`; the session lock is not the bug there | Grepped `withAdvisoryLock(` call sites and read what the callbacks run | **Task 7 split.** 7a: fix the one genuinely broken site (`databaseStorage.ts:3846-3859`, acquire and release on two independent pool round-trips). 7b: the long-job locks need a design decision — pinned session lock vs a lease table — not a mechanical conversion. Also noted: xact-scoping the whole migration pass would directly conflict with Task 2a's non-transactional pragma |

| **D-6** | **Task 8 would have deleted product data.** It specified a 90-day retention prune on `geo_rankings` (21,403 rows), `competitor_geo_rankings` (72,209) and `competitor_citation_snapshots` (29,957), copying the pattern used for `geo_signal_runs`. But those three are not telemetry — they are the citation history that backs prompt-history, the results view and the competitor leaderboard. `api_costs` is the only one of the four that is genuinely internal telemetry | Considered what reads each table before writing the brief | **Task 8 scope cut to `api_costs` only**, plus the FK indexes and RLS policy fixes. Retention on the three product tables is a **product decision for the owner**, not an agent's call, and is recorded as F-13b |

### Verified correct (checked, not assumed)

| Claim                                                                       | How it was verified                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task 5's `date_trunc('week', checked_at)` is equivalent to the JS bucketing | `weekStartOf` (`dashboard.ts:1011-1017`) buckets in **UTC**, Monday-anchored. `checked_at` is `timestamp without time zone`; `db.ts:8-15` forces OID 1114 to parse as UTC. Postgres `date_trunc('week', …)` is ISO Monday-anchored on the same naive value. Equivalent. |
| Task 1's `targetWhere` matches the partial index                            | Live `pg_indexes`: `geo_rankings_run_prompt_platform_uniq … WHERE ((run_id IS NOT NULL) AND (brand_prompt_id IS NOT NULL))` — identical to the code.                                                                                                                    |
