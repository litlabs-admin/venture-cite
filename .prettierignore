node_modules
dist
build
out
coverage
server/public
*.tsbuildinfo
package-lock.json
drizzle/meta
.husky
audit
PHASE1_FEATURES.md
PHASE2_FEATURES.md
PRODUCTION_READINESS_AUDIT.md
AUDIT.md
VentureCite_Phase1_PRD.md

# Standalone Next.js reference build for /home2 - own toolchain.
trakkr.ai

# TanStack Router codegen. Its own header says to exclude it from linters
# and formatters: the plugin regenerates it with its own style, so letting
# prettier reformat it produces a permanent diff that reappears on every
# dev-server start.
src/routeTree.gen.ts
