node_modules
dist
build
out
coverage
server/public
*.tsbuildinfo
package-lock.json
drizzle/meta
supabase/.branches
supabase/.temp
.husky
audit
PHASE1_FEATURES.md
PHASE2_FEATURES.md
PRODUCTION_READINESS_AUDIT.md
AUDIT.md
VentureCite_Phase1_PRD.md

# Standalone Next.js reference build for /home2 - own toolchain.
trakkr.ai

# TanStack Router codegen. Its own header says to exclude it from linters
# and formatters: the plugin regenerates it with its own style, so letting
# prettier reformat it produces a permanent diff that reappears on every
# dev-server start.
src/routeTree.gen.ts

# Vendored agent skills, pinned to upstream revisions in each SOURCE.md.
# Reformatting them creates a permanent diff against upstream and makes the
# pinned revision meaningless. Same reasoning as src/routeTree.gen.ts above.
.agents/skills/

# Claude Code local and generated state. `.claude/skills/` is regenerated by
# `npm run skills:setup`.
.claude/

# B1p audit output. These are evidence written by audit agents, not authored
# source. Reformatting them edits the record.
.audit/B1p/
.audit/B2B3/
